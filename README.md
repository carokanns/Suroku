# Suroku

A simple **Sudoku** puzzle generator and solver implemented in Python with a Tkinter GUI.

## Running

Ensure you have Python 3 installed. Run the application with:

```
python sudoku_gui.py
```

Use the controls at the top of the window to choose a difficulty, start a new game, validate your solution, or reveal the solved board.
